1) Install the requirements "pip install -r requirements.txt"
2) Set path for  "load_tokanizer" and "model" (i.e where you have those 
files in your local machine)
3) To run the script 'python Generate.py "model_name" "path to the imagefile" '
4) model_name can be "Vgg16" or "Inceptionv3" or "Xception"